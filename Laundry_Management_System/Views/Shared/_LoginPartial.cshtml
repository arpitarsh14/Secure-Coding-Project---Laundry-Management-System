@using Microsoft.AspNetCore.Identity
@using Laundry_Management_System.Areas.Identity.Data
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
       @if (!Context.Request.Path.StartsWithSegments("/Identity/Account/Managee", StringComparison.OrdinalIgnoreCase)) // Check if the current page is not the Managee page
        {
            
            <li class="nav-item">
                <a id="manage" class="btn btn-primary" asp-area="Identity" asp-page="/Account/Managee" title="Manage">
                    Edit Profile
                </a>
            </li>
        }
        <a>       </a>
        <li class="nav-item">
            <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button id="logout" type="submit" class="btn btn-primary">Logout</button>
            </form>
        </li>
        
    }
</ul>



